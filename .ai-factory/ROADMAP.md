# Project Roadmap

> Flutter plugin that wraps the Neiry Capsule C SDK — exposes a clean Dart API for BCI device lifecycle, EEG streaming, and all classifiers to mind_mobile.

## Milestones

- [x] **Plugin scaffold** — Flutter plugin boilerplate, pubspec, iOS/Android build configs
- [x] **Dart channel contract** — create `lib/src/channel_names.dart` and export it from `lib/neiry_kit.dart`. File contains: `NeiryChannels` (8 MethodChannel IDs), `NeiryEvents` (28 EventChannel IDs), `DeviceLocatorMethods` / `DeviceMethods` / `ClassifierMethods` / `NFBCalibratorMethods` (all method name strings), `NeiryArgs` (9 argument key strings), `NeiryDeviceType` / `NeiryDeviceMode` / `NeiryConnectionState` enums with int mappings. Add unit tests verifying all strings are unique and enum int values match SDK. Full spec: [`.ai-factory/notes/01-channel-contract.md`](.ai-factory/notes/01-channel-contract.md)
- **Dart models**
  - [x] **sentinel helper + exceptions** — `lib/src/models/internal/sentinel.dart` with `orNull(double?)`, `lib/src/models/neiry_exception.dart` with `NeiryException` base + `BluetoothDisabledException` / `DeviceNotConnectedException` / `CalibrationRequiredException`; `NeiryErrorCode` enum (16 values). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **scalar + signal models** — `DeviceInfo` (no nulls), `NeiryError` (for exception construction), `NfbUserState` / `PhysiologicalStatesValue` / `EmotionsStates` (all use `orNull` sentinel pattern on float fields). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **productivity models** — `ProductivityMetrics` (11 nullable floats + `Uint8List?` artifacts, `fatigueGrowthRate` int enum), `ProductivityIndexes` (6 nullable floats + `relaxation`/`stress` int enums + `hasArtifacts` bool), `ProductivityBaselines` (6 nullable floats). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **calibration models** — `IndividualNfbData` (all non-nullable, defaults from spec), `CalibrationStage` enum, `CalibrationFailReason` enum; `CalibrationEvent` sealed class with subtypes `CalibrationStageFinished(stage)` and `CalibrationCompleted(data)` — native sends `{'type':'stage'/'done'}` over one EventChannel, Dart deserializes to correct subtype; only `IndividualNfbData` has `fromMap`. Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **export + unit tests** — export all models from `lib/neiry_kit.dart`; tests cover sentinel→null, `Uint8List?` size=0 case, enum int mappings, `CalibrationEvent` dispatch by `type` key, `IndividualNfbData.fromMap` round-trip. Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **PhysiologicalStatesBaselines model + PhysioClassifier fix** — add `lib/src/models/physio_baselines.dart`: `PhysiologicalStatesBaselines` with fields `timestamp: DateTime?`, `alpha/beta/alphaGravity/betaGravity/concentration: double?`, `fromMap` using `orNull` sentinel pattern, `toMap` with keys `ts/alpha/beta/alphaGravity/betaGravity/concentration`; change `PhysioClassifier.calibrated` from `Stream<Uint8List>` to `Stream<PhysiologicalStatesBaselines>`; change `PhysioClassifier.importBaselines(Uint8List)` to `importBaselines(PhysiologicalStatesBaselines)` — passes `baselines.toMap()` to the bridge; export from `lib/neiry_kit.dart`; update iOS and Android bridges to emit baselines as a map of 6 fields (not bytes) from the `CalibratedHandler` callback; add `fromMap` round-trip unit test. Detail: [`.ai-factory/notes/15-explore-example-physio-baselines.md`](.ai-factory/notes/15-explore-example-physio-baselines.md)
- **Dart API**
  - [x] **DeviceLocator** — singleton factory with optional `logDirectory`; `requestDevices({type, searchTime})` returns `Stream<List<DeviceInfo>>` that emits once then completes; `createDevice(serial)`, `update()` (single-threaded mode), `dispose()`. Spec: [`.ai-factory/notes/03-dart-api-device.md`](.ai-factory/notes/03-dart-api-device.md); `requestDevices` must pass arguments through `receiveBroadcastStream()` with no separate MethodChannel call — the native scan starts inside `StreamHandler.onListen`; `createDevice` must invoke on `NeiryChannels.deviceLocator` (not `NeiryChannels.device`) because the C API requires the locator handle; all public methods must call `_checkNotDisposed()` and throw `StateError` when disposed; `requestDevices` must use a `StreamController` internally so `_scanSubscription` is set immediately, enabling cancel-on-overlap and cancel-in-dispose; `StreamController.onCancel` and `onDone` must scope to a local `thisSub` variable and use `identical(_scanSubscription, thisSub)` before clearing the field, so cancelling an old consumer's stream cannot kill a newer active scan; must include `setSingleThreaded(bool)` so `update()` is reachable — C SDK defaults to multi-threaded mode where `Update()` is a no-op without `SetSingleThreaded(true)`
  - [x] **Device** — lifecycle (`connect/disconnect/start/stop`); state streams (`connectionStateStream`, `modeChangedStream`); data streams (`eegStream`, `psdStream`, `artifactsStream`, `resistanceStream`, `batteryStream`, `errorStream`); getters (`getInfo`, `battery`, `mode`, `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`, `getChannelNames`, `getChannelIndex`); enforces state machine invariants (`start()` before classifiers, `connect()` before `start()`). Spec: [`.ai-factory/notes/03-dart-api-device.md`](.ai-factory/notes/03-dart-api-device.md)
  - [x] **NfbClassifier** — `factory NfbClassifier(Device, {IndividualNfbData?})`; streams: `stateStream`, `errorStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **PhysioClassifier** — `factory PhysioClassifier(Device)`; `startBaselineCalibration()`, `importBaselines(Uint8List)`; streams: `stateStream`, `calibrationProgress`, `calibrated`, `individualNfbStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **EmotionsClassifier** — `factory EmotionsClassifier(Device)`; streams: `stateStream`, `errorStream`; no calibration required. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **ProductivityClassifier** — two factory paths (`ProductivityClassifier(Device)`, `ProductivityClassifier.withCalibration(Device, IndividualNfbData)`); `startBaselineCalibration()`, `importBaselines()`, `resetAccumulatedFatigue()`; streams: `baselineStream`, `indexesStream`, `metricsStream`, `calibrationProgress`, `calibrated`, `individualNfbStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **CardioClassifier** — two factory paths (`CardioClassifier(Device)`, `CardioClassifier.withCalibration(Device, IndividualNfbData)`); streams: `stateStream` (`CardioData`: heartRate, stressIndex, kaplanIndex, quality flags), `ppgStream`, `errorStream`; pure C API (`clCCardio_*`). Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **NfbCalibrator** — static class; `calibrateIndividual()` → `Stream<CalibrationEvent>`; `calibrateIndividualQuick()` → `Future<IndividualNfbData>`; `importCalibrationData()`, `getCalibrationData()`, `isCalibrated()`. Spec: [`.ai-factory/notes/05-nfb-calibration.md`](.ai-factory/notes/05-nfb-calibration.md)
  - [x] **export + integration tests** — export all classes; tests: state machine throws on wrong order, stream types match models, `CalibrationEvent` sealed dispatch

- **iOS bridges**
  - [x] **bridge setup + spike** — create `ios/Classes/NeiryKitBridge.h` (umbrella header with `#include "CCapsuleAPI.h"`); add `s.public_header_files` to podspec; rewrite `NeiryKitPlugin.swift` to register all 8 MethodChannels + 22 EventChannels with bridge stubs; call `clCCapsule_GetVersionString()` to verify compilation. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceLocatorBridge** — `OpaquePointer?` to `clCDeviceLocator`; methods: `requestDevices`, `createDevice`, `setSingleThreaded`, `dispose`; EventChannel `deviceList` with `DispatchQueue.main.async` dispatch; `deinit` → `clCDeviceLocator_Destroy`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceBridge — lifecycle + getters** — `connect/disconnect/start/stop`; getters: `getInfo`, `getBatteryCharge` (takes `clCError*`), `getMode` (no `clCError*` — must NOT be wrapped in `do/catch checkCError`), `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`; channel name accessors require two-step `clCDevice_GetChannelNames` handle before calling `GetChannelIndexByName`/`GetChannelNameByIndex`/`GetChannelsCount`; `connect` extracts `bipolarChannels` from args in addition to `serial`; device handle cleanup coordinated through `NeiryKitPlugin` — bridges must never cross-call each other; supports only one active device at a time; `deinit` → `clCDevice_Release`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceBridge — streams** — 8 EventChannels: `eeg`, `psd`, `artifacts`, `resistance`, `battery`, `error`, `connectionStatus`, `mode`; stream handlers must be per-channel `DeviceStreamHandler` inner classes so `onListen` receives the channel ID; all C callbacks dispatch to `DispatchQueue.main.async`; all C accessor functions inside callbacks take `clCError*` including `clCPSDData_HasIndividualAlpha/Beta(data, &error)` — if any accessor fails, skip the entire event. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **NfbBridge** — `clCNFB` handle; EventChannels: `nfbState` (NfbUserState fields), `nfbError`; `clCNFB_Create` and `clCNFB_CreateCalibrated` (takes calibrator handle from `clCNFBCalibrator_CreateOrGet`); no Destroy; MethodChannel dispatch shared on `neiry_kit/device`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **NfbCalibratorBridge** — `clCNFBCalibrator_CreateOrGet(device)` — no `clCError*`, returns cached handle, do NOT destroy; separate MethodChannel `neiry_kit/nfb_calibrator` + EventChannel `neiry_kit/events/nfbCalibration`; stage-finished callback receives only handle with no stage number — bridge maintains `currentStage: Int`, increments on each callback and manually calls next stage; `calibrateIndividualQuick` holds MethodChannel `result` sink and calls it from `onCalibrated` — do NOT emit on EventChannel for quick mode; `IsCalibrated`/`HasCalibrationFailed` return bool with no `clCError*` — do NOT wrap in `do/catch checkCError`; calibration EventChannel emits `{'type':'stage','stage':N}` and `{'type':'done',...IndividualNfbData}`. Detail: [`.ai-factory/notes/08-explore-nfb-calibrator.md`](.ai-factory/notes/08-explore-nfb-calibrator.md)
  - [x] **EmotionsBridge** — `clCEmotions` handle; EventChannels: `emotionsState` (EmotionsStates fields), `emotionsError`; `clCEmotions_SetOn*Event` take NO `clCError*` — do NOT wrap in `do/catch checkCError`; no calibration, no Destroy. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **PhysioBridge** — `clCPhysiologicalStates` handle; `clCPhysiologicalStates_SetOn*Event` ALL take `clCError*` — wrap in `do/catch checkCError`; `StartBaselineCalibration` has no error param; `ImportBaselines` takes `const clCPhysiologicalStates_Baselines*` struct — bridge deserializes Dart map (6 keys: `ts/alpha/beta/alphaGravity/betaGravity/concentration`) to C struct; `CalibratedHandler` callback serializes all 6 struct fields to Dart map; `IndividualNFBUpdateHandler` receives only handle with no data — emit empty signal map `{}`; `PhysiologicalStates_Value` has `nfbArtifacts` + `cardioArtifacts` bool fields — include in emitted map; no Destroy. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **CardioBridge — post-review fix** — one bug passed review incorrectly: PPG callback (`CardioBridge.swift`) does not guard `ppgData` for nil before passing to `clCPPGTimedData_GetCount` — Swift imports the C callback param as `OpaquePointer?`, nil would trap at runtime; add `let ppgData = ppgData` to the existing `guard let bridge` statement in the `SetOnPPGDataEvent` closure.
  - [x] **CardioBridge** — `clCCardio` handle; two factory paths: `clCCardio_Create` and `clCCardio_CreateCalibrated(device, calibrator)` where calibrator = `clCNFBCalibrator_CreateOrGet`; EventChannels: `cardioState` (direct struct fields: `heartRate/stressIndex/kaplanIndex/hasArtifacts/skinContact/motionArtifacts/metricsAvailable`), `cardioPpg` (PPG uses accessor functions: `GetCount/GetValue/GetTimestampMilli`), `cardioError`; `CardioData` bool fields initialized to `0.F` — read normally; `SetOnIndexesUpdateEvent` and `SetOnCalibratedEvent` take `clCError*`; no Destroy. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)
  - [x] **ProductivityBridge** — `clCProductivity` handle; two factory paths: `clCProductivity_Create` and `clCProductivity_CreateWithIndividualData(device, &data)` — bridge converts Dart `IndividualNfbData` map back to C struct; EventChannels: `productivityMetrics` (`clCProductivity_Metrics`: 11 floats + `artifactsData` as `Uint8List`), `productivityIndexes` (`clCProductivity_Indexes`: relaxation enum 0–5, stress enum 0–2), `productivityBaseline`, `productivityCalibrationProgress`, `productivityCalibrated`, `productivityIndividualNfb`; ALL `clCProductivity_SetOn*Event` take NO `clCError*`; `StartBaselineCalibration` also has no error param; `resetAccumulatedFatigue` takes `clCError*`; no Destroy. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)

- **Android bridges**
  - [x] **build setup + spike** — Path B (custom JNI wrapper): extract `libCapsuleClient.so` + `libc++_shared.so` from `CapsuleService.aar/jni/arm64-v8a/` to `android/src/main/jniLibs/arm64-v8a/`; keep `fileTree` dependency for both AARs; add `CMakeLists.txt` building `neiry_jni` shared lib linking against extracted `.so`; add `externalNativeBuild` block to `build.gradle.kts`; add `external fun nativeGetVersion(): String` to `NeiryKitPlugin.kt`; call `System.loadLibrary("neiry_jni")` in companion object; implement `nativeGetVersion()` JNI stub in `jni_bridge.cpp` calling `clCCapsule_GetVersionString()` to verify linking; register all 8 MethodChannels + 22 EventChannels with stubs. `devicedriver.aar` is pure Kotlin — no `.so` extraction needed. arm64-v8a only — no simulator. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceLocatorBridge** — `jni_device_locator.cpp` + `DeviceLocatorBridge.kt`; `jlong` handle; methods: `requestDevices`, `createDevice`, `setSingleThreaded`, `dispose`; EventChannel `deviceList` via `Handler(Looper.getMainLooper()).post`; all `external fun` declarations on a single `NativeBridge` class (not on `DeviceLocatorBridge`) — centralized per notes/11; `FlutterError(code, message, details): Exception` must be defined as a shared class (Android has no built-in FlutterError throwable); error propagation: JNI encodes `clCError` as `"<code>|<message>"` string, Kotlin splits on first `|` via top-level `parseSdkError()` to get numeric code for `result.error(code, message, details)` — preserves `NeiryErrorCode` mapping on Dart side; `g_deviceListCallback` guarded by `pthread_mutex_t` — copy to local under lock before JNI calls, null-check after; `DetachCurrentThread` called on ALL return paths in C callback including early-return when callback is NULL — use `goto cleanup`; `marshal_device_list` checks `clCError*` on every C SDK call (`GetCount`, `GetDeviceInfo`), skips entries on failure; C function is `clCDeviceInfo_GetType` (NOT `clCDeviceInfo_GetDeviceType`); `FindClass` paths use literal `neiry_kit` (NOT `neiry_1kit` — `_1` encoding applies only to `Java_` function name prefixes, not `FindClass`); `createDevice` and `onListen` must guard `handle != 0L` and throw `FlutterError("NO_LOCATOR", ...)` when locator not created; `g_jvm` initialized in `nativeCreate` (first JNI call in lifecycle), not lazily; null-safe dispatch in plugin via `val bridge = deviceLocatorBridge ?: return result.error("NOT_INITIALIZED", ...)`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [x] **DeviceLocatorBridge — post-review fixes** — two issues that passed code review incorrectly: (1) in `on_device_list_callback` (`jni_device_locator.cpp`), replace raw pointer copy of `g_deviceListSink`/`g_handler` under mutex with `NewLocalRef` — raw copy is a use-after-free if `DeleteGlobalRef` runs concurrently on `nativeSetDeviceListSink(null)`; delete local refs after use; (2) `DeviceLocatorBridge.update()` must throw `FlutterError("NO_LOCATOR", "DeviceLocator not created", null)` when `handle == 0L` — iOS parity: `DeviceLocatorBridge.swift` throws when `locator` is nil, Android must match.
  - [x] **DeviceBridge — lifecycle + getters** — `jni_device.cpp` + `DeviceBridge.kt`; `connect/disconnect/start/stop`; getters: `getBatteryCharge`, `getMode` (no `clCError*`), `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`; channel names require two-step `clCDevice_GetChannelNames` handle pattern; device handle as `jlong` cast via `(jlong)(uintptr_t)`; `connect` extracts `bipolarChannels` from args; all `external fun` on `NativeBridge` (established in DeviceLocatorBridge); reuse `FlutterError` + `parseSdkError()` from DeviceLocatorBridge; null-safe dispatch in plugin via `val bridge = deviceBridge ?: return result.error("NOT_INITIALIZED", ...)`; `FindClass` paths use literal `neiry_kit` (not `neiry_1kit`). Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [x] **DeviceBridge — streams** — 8 EventChannels: `eeg`, `psd`, `artifacts`, `resistance`, `battery`, `error`, `connectionStatus`, `mode`; C callbacks have no context param — use static device registry keyed by `clCDevice` pointer; all callbacks post to `Handler(Looper.getMainLooper()).post`; PSD accessors including `HasIndividualAlpha/Beta` all take `clCError*` — skip event on error; global callback refs (EventSinks) guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy) to prevent use-after-free if `DeleteGlobalRef` runs concurrently; delete local refs after use; `DetachCurrentThread` called on ALL return paths in C callbacks — use `goto cleanup`; `FindClass` paths use literal `neiry_kit`. JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [x] **NfbBridge** — `jni_nfb.cpp` + `NfbBridge.kt`; EventChannels: `nfbState`, `nfbError`; two factory paths mirroring iOS; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths; prerequisite: remove `static` from all map helpers and cache vars in `jni_device.cpp` so `jni_nfb.cpp` can `extern` them; `clCNFBCalibrator_CreateOrGet` returns nullable — null-check and throw `"0|clCNFBCalibrator_CreateOrGet returned null"` if nil; `clCIndividualNFBData` has 10 fields including `individualPeakFrequency` — all 10 must be populated; `failReason` casts to `clCIndividualNFBCalibrationFailReason` (no underscore after `clC`); use two separate JNI functions `nativeSetNfbStateSink` / `nativeSetNfbErrorSink` (not one with streamType dispatch); teardown order: NfbBridge before DeviceBridge and DeviceLocatorBridge. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **NfbCalibratorBridge** — `jni_nfb_calibrator.cpp` + `NfbCalibratorBridge.kt`; same stage-tracking, quick-mode, and no-Destroy rules as iOS; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink ref guarded by `pthread_mutex_t`; `DetachCurrentThread` on all callback return paths; map helpers (`make_map`, `map_put_*`) and JNI cache vars are already non-static in `jni_device.cpp` after NfbBridge — declare them as `extern` in `jni_nfb_calibrator.cpp`, do NOT redefine; `clCNFBCalibrator_CreateOrGet` returns nullable handle — null-check and throw `"0|clCNFBCalibrator_CreateOrGet returned null"` before use; `IsCalibrated`/`HasCalibrationFailed` have no `clCError*` — do NOT wrap in error check. Detail: [`.ai-factory/notes/08-explore-nfb-calibrator.md`](.ai-factory/notes/08-explore-nfb-calibrator.md)
  - [x] **EmotionsBridge** — `jni_emotions.cpp` + `EmotionsBridge.kt`; `clCEmotions_SetOn*Event` take NO `clCError*`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths; map helpers (`make_map`, `map_put_*`) and JNI cache vars declared as `extern` from `jni_device.cpp` — do NOT redefine. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **PhysioBridge** — `jni_physio.cpp` + `PhysioBridge.kt`; `clCPhysiologicalStates_SetOn*Event` ALL take `clCError*`; baselines serialized as map of 6 floats (same keys as iOS); `IndividualNFBUpdateHandler` emits `{}`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths; map helpers (`make_map`, `map_put_*`) and JNI cache vars declared as `extern` from `jni_device.cpp` — do NOT redefine. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **CardioBridge** — `jni_cardio.cpp` + `CardioBridge.kt`; two factory paths (plain + calibrated via `clCNFBCalibrator_CreateOrGet`); EventChannels: `cardioState`, `cardioPpg` (PPG accessor pattern), `cardioError`; `CardioData` bool fields cast `(jboolean)(data->field != 0)`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths; `clCNFBCalibrator_CreateOrGet` returns nullable — null-check and throw `"0|clCNFBCalibrator_CreateOrGet returned null"` before use in calibrated factory path; map helpers (`make_map`, `map_put_*`) and JNI cache vars declared as `extern` from `jni_device.cpp` — do NOT redefine. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)
  - [x] **ProductivityBridge** — `jni_productivity.cpp` + `ProductivityBridge.kt`; two factory paths (plain + `CreateWithIndividualData` — convert Dart map back to C struct); EventChannels: `productivityMetrics`, `productivityIndexes`, `productivityBaseline`, `productivityCalibrationProgress`, `productivityCalibrated`, `productivityIndividualNfb`; ALL `SetOn*Event` take NO `clCError*`; `resetAccumulatedFatigue` takes `clCError*`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths; `clCIndividualNFBData` has 10 fields including `individualPeakFrequency` — all 10 must be populated when converting Dart map to C struct; map helpers (`make_map`, `map_put_*`) and JNI cache vars declared as `extern` from `jni_device.cpp` — do NOT redefine. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)

- **SDK v2.0.72 upgrade**
  - [x] **Replace vendored binaries** — copy `official/Capsule v2.0.72/CapsuleAPI/iOS/CapsuleClient.framework` over `official/iOS/CapsuleClient.framework`; copy `official/Capsule v2.0.72/CapsuleAPI/Android/CapsuleService.aar` and `devicedriver.aar` over `official/Android/`; extract updated `libCapsuleClient.so` from the new `CapsuleService.aar` (`jni/arm64-v8a/`) and replace `android/src/main/jniLibs/arm64-v8a/libCapsuleClient.so`; no code changes required — C API is binary-compatible. Detail: [`.ai-factory/notes/18-sdk-v2-0-72-c-api-diff.md`](.ai-factory/notes/18-sdk-v2-0-72-c-api-diff.md)
  - [x] **firmwareVersion getter — Dart + iOS + Android** — new function in v2.0.72: `clCDevice_GetFirmwareVersion(clCDevice, clCError*) → const char*`; only valid after device connected. Add `static const getFirmwareVersion = 'getFirmwareVersion'` to `DeviceMethods` in `channel_names.dart`; add `Future<String> getFirmwareVersion()` to `lib/src/api/device.dart` — invokes `DeviceMethods.getFirmwareVersion` on `NeiryChannels.device`, returns `String`; iOS: add case in `DeviceBridge.swift` calling `clCDevice_GetFirmwareVersion` wrapped in `checkCError`, return result as `String`; Android: add `external fun nativeGetFirmwareVersion(handle: Long): String` to `NativeBridge`, implement in `jni_device.cpp` using `clCDevice_GetFirmwareVersion` with error check, encode error as `"<code>|<message>"` string and throw on failure.

- **MEMS classifier**
  - [x] **MemsData model** — create `lib/src/models/mems_data.dart`: immutable `MemsSample` with fields `accelerometer: ({double x, double y, double z})`, `gyroscope: ({double x, double y, double z})`, `timestamp: DateTime`; `fromMap` factory reading keys `ax/ay/az/gx/gy/gz/ts` (ts as int milliseconds → `DateTime.fromMillisecondsSinceEpoch`); no sentinel/null fields (MEMS data is always valid when received); export from `lib/neiry_kit.dart`. C source: `clCPoint3d` has fields `x/y/z` as float. Note: `memsData` EventChannel ID already exists in `NeiryEvents` — do not add a duplicate.
  - [x] **MEMSClassifier Dart API** — create `lib/src/api/classifiers/mems_classifier.dart`: `class MEMSClassifier` (not static, owns lifecycle); `factory MEMSClassifier(Device device)` — invokes `ClassifierMethods.create` on `NeiryChannels.mems` (add `mems = 'neiry_kit/mems'` to `NeiryChannels`); `factory MEMSClassifier.withCalibration(Device device, IndividualNfbData data)` — invokes `ClassifierMethods.createCalibrated` passing `data.toMap()`; `Stream<List<MemsSample>> get memsStream` — listens to `NeiryEvents.memsData`, maps raw list to `List<MemsSample>`; `void dispose()` — invokes `ClassifierMethods.dispose`; enforces `device.isStarted` before construction (throw `StateError` if not started); export from `lib/neiry_kit.dart`. Also add `static const mems = 'neiry_kit/mems'` to `NeiryChannels`.
  - [x] **MEMSBridge iOS** — create `ios/Classes/classifiers/MemsBridge.swift`; `clCMEMS` handle; two factory paths: `clCMEMS_Create(device, &error)` and `clCMEMS_CreateCalibrated(device, calibrator, &error)` where calibrator = `clCNFBCalibrator_CreateOrGet(device)`; subscribe `clCMEMS_SetOnMEMSTimedDataUpdateEvent` with `clCError*`; callback serializes each sample to map `{ax, ay, az, gx, gy, gz, ts}` using `clCMEMSTimedData_GetCount/GetAccelerometer/GetGyroscope/GetTimestampMilli` — no `clCError*` on any data accessor; dispatch callback via `DispatchQueue.main.async` before calling sink; register MethodChannel `neiry_kit/mems` in `NeiryKitPlugin.swift` for `create`/`createCalibrated`/`dispose`; register EventChannel `neiry_kit/events/memsData` for the data stream; no Destroy call needed (SDK manages lifetime). `clCPoint3d` fields accessed as `.x/.y/.z`.
  - [x] **MEMSBridge Android** — create `android/src/main/cpp/jni_mems.cpp` + `android/src/main/kotlin/.../MemsBridge.kt`; `jlong` handle; same two factory paths as iOS using `clCMEMS_Create` / `clCMEMS_CreateCalibrated`; `clCNFBCalibrator_CreateOrGet` nullable — null-check and throw `"0|clCNFBCalibrator_CreateOrGet returned null"`; callback registered via `clCMEMS_SetOnMEMSTimedDataUpdateEvent` with `clCError*`; callback iterates samples with `GetCount/GetAccelerometer/GetGyroscope/GetTimestampMilli` — no `clCError*` on data accessors; serialize each sample to Dart map with keys `ax/ay/az/gx/gy/gz/ts`; dispatch via `Handler(Looper.getMainLooper()).post`; global EventSink ref guarded by `pthread_mutex_t` — use `NewLocalRef` under lock; `DetachCurrentThread` on all callback return paths; `make_map`/`map_put_*` and JNI cache vars declared as `extern` from `jni_device.cpp` — do NOT redefine; all `external fun` on `NativeBridge`; register MethodChannel `neiry_kit/mems` + EventChannel `neiry_kit/events/memsData` in `NeiryKitPlugin.kt`.

- **Example app**
  - [x] **app scaffold** — add `riverpod: ^3.3.0`, `flutter_riverpod: ^3.3.0`, `go_router: ^14.0.0`, `rxdart: ^0.27.0`, `file_picker: ^8.0.0`, `path_provider: ^2.1.5`, `wakelock_plus: ^1.2.0` to `example/pubspec.yaml`; wrap `main.dart` in `ProviderScope`; use `StatefulShellRoute.indexedStack` (NOT `ShellRoute`) — each branch gets its own `Navigator`, keeping all tab widgets mounted so live streams survive tab switches; 4 tabs: `/device`, `/streams`, `/classifiers`, `/calibration`; the 5 UI sections from later milestones nest inside these 4 tabs (Productivity+Cardio inside `/classifiers` as sub-page); use `NavigationBar` (Material 3); create `deviceLocatorProvider` as `NotifierProvider` with `ref.onDispose`; all screens empty `Scaffold` stubs. Detail: [`.ai-factory/notes/12-explore-example-navigation.md`](.ai-factory/notes/12-explore-example-navigation.md)
  - [x] **Device tab** — `deviceScanProvider` as `FutureProvider.family` wrapping `requestDevices().first` (one-shot stream, NOT `StreamProvider`); `activeDeviceProvider` as `NotifierProvider<Device?>` — stores single device, enforces one-device-at-a-time; `deviceConnectionStateProvider` as `StreamProvider`; `deviceIsStartedProvider` as `StateProvider<bool>` — set manually on `start()`/`stop()` (not from stream); `deviceUiStateProvider` composites both into `DeviceUiState` enum (`idle/connected/started`); button enabled state derived from composite; all stream subscriptions in `NotifierProvider`, NOT in `StreamBuilder` widgets (streams must survive tab switches via `StatefulShellRoute`); async disposal: `stop()` → `disconnect()` → `dispose()` order enforced in app `State.dispose()` before `ProviderContainer.dispose()` (`ref.onDispose` is synchronous — cannot await). Detail: [`.ai-factory/notes/13-explore-example-device-providers.md`](.ai-factory/notes/13-explore-example-device-providers.md)
  - [x] **Streams tab** — `eegProvider` as throttled `StreamProvider` (`rxdart` `throttleTime(100ms)` → 10 Hz); `psdProvider` throttled at 500ms; `resistanceMapProvider` as `NotifierProvider<Map<String, double>>` accumulating per-channel events (resistance fires per-channel independently, not as a batch); `batteryProvider` throttled at 1000ms; `artifactsProvider` unthrottled (bool flags need immediate feedback); all subscriptions in providers NOT `StreamBuilder` widgets; EEG display shows last sample per channel + channel count; PSD shows band powers (delta/theta/alpha/smr/beta); resistance shows channel→kΩ table with color indicator (green if < 500 kΩ); throttle BEFORE any expensive map/transform operations. Detail: [`.ai-factory/notes/16-explore-example-streams-tab.md`](.ai-factory/notes/16-explore-example-streams-tab.md)
  - [x] **NFB tab + calibration** — `CalibrationNotifier` as `AsyncNotifier<IndividualNfbData?>` handling full (stream) + quick (future) + import/export; `CalibrationTimerNotifier` as `StateNotifier` with `Timer.periodic` — timer is purely client-side, `CalibrationEvent` stream emits only on stage END not ticks; start timer for stage 1 at call time (before first event), reset on each `CalibrationStageFinished`; `CalibrationUiState` as sealed class (`CalibrationIdle / CalibrationStageActive(stage, elapsed) / CalibrationDone / CalibrationError`); stage instruction text: stage1+3 = "Close eyes and relax", stage2+4 = "Open eyes, look straight ahead"; `wakelock_plus` enabled before calibration, disabled in finally/abort; `NfbClassifier` provider returns null if device not started — widget shows "Waiting for device"; completed `IndividualNfbData` stored in shared `nfbCalibrationProvider` so Productivity+Cardio tab can use it; Import/Export via `file_picker` + `path_provider` as JSON (`data.toMap()`); `_sub` cleanup via `ref.onDispose()` inside `build()` — `AsyncNotifier` has no overrideable `dispose()`; disable wakelock in `finally` block wrapping the guarded future so it is released on both success and error; write `nfbCalibrationProvider` conditionally after `AsyncValue.guard` only when `state.hasValue && state.value != null`; guard stage advance before array access — `if (stage.code < 3)` then `CalibrationStage.fromCode(stage.code + 1)` — do NOT follow explore note order which crashes on stage4; store completer as `_fullCompleter` field, `abort()` must complete it with error before nulling; `NfbClassifierNotifier.onDispose` must capture the local classifier instance, not read `state`. Detail: [`.ai-factory/notes/14-explore-example-calibration-ui.md`](.ai-factory/notes/14-explore-example-calibration-ui.md)
  - [x] **Physio + Emotions tab** — requires **PhysiologicalStatesBaselines model + PhysioClassifier fix** milestone above; Import/Export baselines as JSON (`PhysiologicalStatesBaselines.toMap()`/`fromMap()`); Physio fires every ~2 min — show "Last updated: HH:MM:SS" label, reduce opacity to 0.5 while waiting for first update; `PhysiologicalStatesValue` has `nfbArtifacts` + `cardioArtifacts` bool fields — show as "Signal Quality" row with green/red icons below numeric values; Emotions fires continuously — separate `Card` above Physio card with no timestamp. Detail: [`.ai-factory/notes/15-explore-example-physio-baselines.md`](.ai-factory/notes/15-explore-example-physio-baselines.md)
  - [x] **Productivity + Cardio tab** — shared `nfbCalibrationProvider` as `StateNotifierProvider<IndividualNfbData?>` — set by Calibration tab, watched here; factory `Switch` disabled if `nfbCalibrationProvider` is null; toggling switch invalidates classifier provider to recreate (safe: classifiers have no Destroy, creating a new instance while old exists is OK — SDK manages handles); `ProductivityIndexes.relaxation` = `clCProductivity_RecommendationValue` enum (0–5: NoRecommendation/Involvement/Relaxation/SlightFatigue/SevereFatigue/ChronicFatigue) — show label, not int; `ProductivityIndexes.stress` = `clCProductivity_StressValue` (0–2: NoStress/Anxiety/Stress) — show label; `ProductivityMetrics.artifactsData` is opaque blob — show size only ("Artifacts: N bytes" or "none"); PPG last value = `ppgData.values.last` + `ppgData.timestamps.last` as formatted time; all classifier providers return null when device not started. Detail: [`.ai-factory/notes/17-explore-example-productivity-cardio-tab.md`](.ai-factory/notes/17-explore-example-productivity-cardio-tab.md)
  - [x] **MEMS tab in example app** — add a 5th tab `/mems` to `StatefulShellRoute`; `memsProvider` as `StreamProvider` throttled with `rxdart throttleTime(100ms)` — 10 Hz is sufficient for display; show the last sample's accelerometer (x/y/z) and gyroscope (x/y/z) in two `Card` widgets; `memsClassifierProvider` as `NotifierProvider<MEMSClassifier?>` — instantiated via `MEMSClassifier(device)` when device is started, null otherwise; `Switch` to toggle `MEMSClassifier.withCalibration` (enabled only when `nfbCalibrationProvider != null`); toggling the switch invalidates the provider to recreate the classifier.
  - [x] **Calibration data card** — `example/lib/screens/calibration_screen.dart` shows `_CalibrationCard` and `_NfbCard` but has no card displaying the `IndividualNfbData` fields. Add a third `_CalibrationDataCard` `ConsumerWidget` inserted after `_NfbCard` in `CalibrationScreen.build`. The widget watches `calibrationProvider` and derives display data as `calibrationProvider.value ?? const IndividualNfbData()` — default-constructed `IndividualNfbData` serves as the pre-calibration placeholder (defaults: `individualFrequency: 10.0`, `lowerFrequency: 7.0`, `upperFrequency: 13.0`, etc.). Layout mirrors `_NfbCard` exactly: a `Card` → `Padding(16)` → `Column` with bold title `'Individual Alpha'` followed by five `_BandRow` rows: `individualFrequency` (label `'Peak, Hz'`), `lowerFrequency` (label `'Lower, Hz'`), `upperFrequency` (label `'Upper, Hz'`), `individualBandwidth` (label `'Bandwidth, Hz'`), `individualNormalizedPower` (label `'Norm. power'`). Do not show `individualPeakFrequencyPower` (absolute uV²/Hz) or `individualPeakFrequencySuppression`. Reuse the existing `_BandRow` widget without modification — it already handles `double?` with `—` fallback, but here all values are non-null so `—` will never appear. [5m 35s]
  - [x] **BLE runtime permissions on Scan** — `example/pubspec.yaml` has no `permission_handler` dependency and `_DeviceScreenState._scan()` in `example/lib/screens/device_screen.dart` triggers `deviceScanProvider` without checking BLE permissions, causing `SecurityException: Need ACCESS_FINE_LOCATION` on Android 6–11. Add `permission_handler: ^11.3.1` to `example/pubspec.yaml`; in `_scan()`, before the existing invalidate/setState logic, call an async `_checkAndRequestPermissions()` helper that requests `[Permission.bluetoothScan, Permission.bluetoothConnect]` on Android 12+ (API 31+) or `[Permission.locationWhenInUse]` on Android 6–11; if any requested permission is permanently denied call `openAppSettings()` and return; if any is denied (not permanently) call `_showError('Bluetooth permissions required')` and return; only proceed to invalidate/setState when all permissions are granted. For iOS: add `NSBluetoothAlwaysUsageDescription` with value `"This app uses Bluetooth to connect to Neiry EEG devices."` to `example/ios/Runner/Info.plist` — required for iOS 13+, without it CBCentralManager crashes on first use; no runtime permission call needed on iOS, the OS shows the dialog automatically. [20m 0s]

- **Bug fixes & hardening**

  - [x] **Handle unknown NeiryDeviceMode codes from Android SDK** — after `connect()` on Android, `modeChangedStream` throws `Unhandled Exception: Invalid argument(s): Unknown NeiryDeviceMode code: 7`. Investigation confirms: the public C API enum `clCDevice_Mode` defines values 0–6 (`Resistance`…`StopPPG`), but the Android AAR v2.0.72 contains a separate internal `Device_Status` enum (visible in the Python wrapper: `Invalid=0, PowerDown=1, Idle=2, Signal=3, Resist=4, SignalResist=5, Envelope=6`) that the native layer occasionally fires through the `SetOnModeSwitchedEvent` callback instead of the public Mode enum — including undocumented values like `7`. Code `7` arrives transiently during GATT discovery immediately after `connect()`, before `Start()` is called; it is not a stable operating mode. Fix in `lib/src/channel/enums.dart`: change `NeiryDeviceMode.fromCode` to return `null` for unrecognised codes (change return type to `NeiryDeviceMode?`). Fix in `lib/src/api/device.dart:67`: add `.whereType<NeiryDeviceMode>()` after the `.map(fromCode)` call so unknown codes are silently dropped and the public stream type stays `Stream<NeiryDeviceMode>`. Do NOT add `unknown7(7)` or any placeholder values to the enum — future SDK versions may emit additional undocumented codes and the defensive filter handles all of them at once. [11m 47s]
  - [x] **Fix "module already exists" crash when toggling Use NFB Calibration** — `ProductivityClassifier`, `CardioClassifier`, and `MEMSClassifier` crash with `Fatal signal 64` / `clCMEMS module already exists` on Android when the "Use NFB Calibration" `Switch` is toggled, because Riverpod calls `build()` on the new notifier — which invokes `clCProductivity_Create` / `clCCardio_Create` / `clCMEMS_Create` on the platform channel — before `ref.onDispose()` on the previous notifier fires its `dispose()`. The SDK enforces a one-instance-per-device rule and aborts on a second `Create` call. Fix in `example/lib/providers/productivity_classifier_provider.dart` (`ProductivityClassifierNotifier.build`), `example/lib/providers/cardio_classifier_provider.dart` (`CardioClassifierNotifier.build`), and `example/lib/providers/mems_classifier_provider.dart` (`MemsClassifierNotifier.build`): before creating the new classifier, read the current `state` (via `ref.state` if available in the notifier context) and call `dispose()` on it synchronously if non-null, then null it, before proceeding to create the new instance. If Riverpod's `Notifier.build` does not expose `state` before first build, introduce an explicit `_current` field and manage the lifecycle manually. Do not touch the toggle guard (`nfbData != null`) or the `ref.onDispose` registration — only the ordering of dispose-before-create. Additionally, note that `nativeCreateProductivityWithIndividualData` on Android silently falls back to a plain `clCProductivity_Create` (the `clCProductivity_CreateWithIndividualData` symbol is absent from the Android AAR) — this is a known SDK limitation; add a `TODO` comment in `jni_productivity.cpp` at line 100 noting the missing symbol and that NFB data is not applied on Android. [34m 21s]
  - [x] **Fix scan spinner not showing on repeat scan** — in `example/lib/screens/device_screen.dart`, `_buildScanResults()` uses `scanAsync.when(loading: …)` to show a `CircularProgressIndicator`. On the first scan this works because `deviceScanProvider` starts as `AsyncLoading`. On repeat scans, `ref.invalidate(deviceScanProvider(params))` transitions the provider to `AsyncData(isLoading: true)` (Riverpod "refreshing" state), not to `AsyncLoading` — so `.when()` routes to the `data:` callback with the previous device list, and the spinner is never shown. Fix: in `_buildScanResults()`, add `if (scanAsync.isLoading) return const Padding(padding: EdgeInsets.symmetric(vertical: 16), child: Center(child: CircularProgressIndicator()));` immediately after `final scanAsync = ref.watch(deviceScanProvider(params));` and before the existing `return scanAsync.when(…)` — this guard fires for both `AsyncLoading` (first scan) and `AsyncData/AsyncError` with `isLoading: true` (repeat scans). No other changes needed. [3m 26s]
  - [ ] **Fix PSD band power display in Streams tab** — `_bandPower()` in `example/lib/screens/streams_screen.dart:78` returns 0 for all bands due to two bugs confirmed by log: (1) `GetBandLower`/`GetBandUpper` arrive swapped — SDK sends `deltaLower=4.0, deltaUpper=1.0` — so the condition `f >= lower && f <= upper` never matches any frequency bin; fix by normalizing bounds in `_bandPower`: replace `f >= lower && f <= upper` with `f >= lower.clamp(double.negativeInfinity, upper) && f <= upper.clamp(lower, double.infinity)` or simply swap if `lower > upper`; (2) PSD values are in SI units (order of 1e-14 to 1e-17 W/Hz), so `.toStringAsFixed(3)` always renders `0.000` — change to `.toStringAsExponential(2)`. Do not touch JNI or iOS bridge serialization — the swapped bounds come from the SDK and are correct as-is (lower/upper are labels, not guarantees of ordering).
  - [ ] **Fix classifier singleton lifecycle — create at connect, not at start** — all 6 classifier `NotifierProvider`s (`ProductivityClassifierNotifier`, `CardioClassifierNotifier`, `MEMSClassifierNotifier`, `EmotionsClassifierNotifier`, `NfbClassifierNotifier`, `PhysioClassifierNotifier`) currently gate creation on `deviceIsStartedProvider` (`if (device == null || !isStarted) return null`), so each `Stop → Start` cycle disposes and re-creates the classifier. The Capsule SDK enforces a one-instance-per-device rule for all classifier modules (`clCCardio`, `clCMEMS`, `clCProductivity`, `clCEmotions`, `clCNFB`, `clCPhysiologicalStates`) — there is no `Release`/`Destroy` function in the C API for any of them; a second `Create` call against the same live device crashes with `clCCardio module already exists` / `Fatal signal 64`. On Android this crash manifests on the second `Start` press (Stop without Disconnect). Additionally, on Android `clCCardio_Create` triggers the PPG hardware mode switch (`on_mode_switched: mode=StartPPG`) internally — if Cardio/MEMS are only created at `Start`, the first run works but LED lights PPG correctly only after the second run; if created at connect they stay live and PPG activates immediately. Fix: (1) In all 6 provider `build()` methods in `example/lib/providers/` (`cardio_classifier_provider.dart`, `mems_classifier_provider.dart`, `productivity_classifier_provider.dart`, `emotions_classifier_provider.dart`, `nfb_classifier_provider.dart`, `physio_classifier_provider.dart`), remove the `ref.watch(deviceIsStartedProvider)` line and change the null-guard to `if (device == null) return null` — classifier is created when device connects, `ref.onDispose` already handles cleanup on disconnect. Remove the now-unused `import 'device_state_providers.dart'` from each file. (2) In all 6 Dart classifier factory constructors (`lib/src/api/classifiers/cardio_classifier.dart`, `mems_classifier.dart`, `productivity_classifier.dart`, `emotions_classifier.dart`, `nfb_classifier.dart`, `physio_classifier.dart`), replace the `if (!device.isStarted)` guard and `StateError('Cannot create … before Device.start()')` with `if (!device.isConnected)` and `StateError('Cannot create … before Device.connect()')`. (3) In `example/lib/screens/mems_screen.dart` and `example/lib/screens/productivity_cardio_screen.dart`, change calibration toggle guard from `uiState != DeviceUiState.started` to `uiState == DeviceUiState.idle` and update subtitle text from `'Stop streaming to change this setting'` to `'Disconnect to change this setting'` — calibration data now takes effect on the next disconnect→reconnect, not stop→start. Do not touch `deviceIsStartedProvider` reads in `device_screen.dart` (start/stop button guards), `DeviceUiState` derivation in `device_state_providers.dart`, or any other non-classifier provider.
  - [ ] **Add permanent action logs to example app** — no button press or tab switch is currently visible in logcat, making manual testing on device impossible to follow without reading source code. Add permanent (non-debug-only) `dart:developer` `log()` calls to every interactive action in the example app using tag `'Neiry'`. Use `dart:developer` `log(message, name: 'Neiry')` — not `debugPrint` (stripped in profile/release) and not `print` (no tag). Tab switches: in `example/lib/router.dart`, wrap `onDestinationSelected` in `_RootScaffold.build` — change `onDestinationSelected: navigationShell.goBranch` to a lambda that calls `log('Tab → ${_tabName(index)}', name: 'Neiry')` then `navigationShell.goBranch(index)`; add a private `_tabName(int i)` helper returning the label strings `['Device', 'Streams', 'Classifiers', 'Productivity', 'MEMS', 'Calibration']`. Device screen (`example/lib/screens/device_screen.dart`): add `log('Scan tapped', name: 'Neiry')` at the start of `_scan()`; add `log('Device selected: $serial', name: 'Neiry')` inside `setState(() => _selectedSerial = d.serial)` in the `ListTile.onTap` lambda in `_buildScanResults()`; add `log('Connect tapped: $_selectedSerial', name: 'Neiry')` at the start of `_connect()`; add `log('Start tapped', name: 'Neiry')` at the start of `_start()`; add `log('Stop tapped', name: 'Neiry')` at the start of `_stop()`; add `log('Disconnect tapped', name: 'Neiry')` at the start of `_disconnect()`. Calibration screen (`example/lib/screens/calibration_screen.dart`): add `log('Calibration: Start Full tapped', name: 'Neiry')` before the `ref.read(calibrationProvider.notifier).startFull()` call in `_IdleContent`; add `log('Calibration: Start Quick tapped', name: 'Neiry')` before `startQuick()`; add `log('Calibration: Import tapped', name: 'Neiry')` before `importFromFile()`; add `log('Calibration: Abort tapped', name: 'Neiry')` before `abort()` in `_ActiveContent`; add `log('Calibration: Export tapped', name: 'Neiry')` before `exportToFile()` in `_DoneContent`; add `log('Calibration: Recalibrate tapped', name: 'Neiry')` before `startFull()` in `_DoneContent`; add `log('Calibration: Retry tapped', name: 'Neiry')` before `ref.invalidate(calibrationProvider)` in `_ErrorContent`. Classifiers screen (`example/lib/screens/classifiers_screen.dart`): add `log('Physio: Start Baseline Calibration tapped', name: 'Neiry')` before `startBaselineCalibration()` call; add `log('Physio: Import Baselines tapped', name: 'Neiry')` before `PhysioBaselinesFileManager.importFromFile()`; add `log('Physio: Export Baselines tapped', name: 'Neiry')` before `PhysioBaselinesFileManager.exportToFile()`. MEMS screen (`example/lib/screens/mems_screen.dart`): add `log('MEMS: Use NFB Calibration toggled: $val', name: 'Neiry')` inside the `SwitchListTile.onChanged` lambda before `ref.read(useMemsCalibrationToggleProvider.notifier).state = val`. Productivity+Cardio screen (`example/lib/screens/productivity_cardio_screen.dart`): add `log('Productivity: Use NFB Calibration toggled: $val', name: 'Neiry')` inside the `SwitchListTile.onChanged` lambda before `ref.read(useCalibrationToggleProvider.notifier).state = val`; add `log('Productivity: Start Baseline Calibration tapped', name: 'Neiry')` before `startBaselineCalibration()`; add `log('Productivity: Reset Fatigue tapped', name: 'Neiry')` before `resetAccumulatedFatigue()`. Do not add logs to the Streams screen (no interactive controls) or to any provider/notifier files. Do not wrap logs in `kDebugMode` — these are permanent diagnostic logs intended for production use. Add `import 'dart:developer';` to each file that does not already have it.
