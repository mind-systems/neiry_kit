# Project Roadmap

> Flutter plugin that wraps the Neiry Capsule C SDK — exposes a clean Dart API for BCI device lifecycle, EEG streaming, and all classifiers to mind_mobile.

## Milestones

- [x] **Plugin scaffold** — Flutter plugin boilerplate, pubspec, iOS/Android build configs
- [x] **Dart channel contract** — create `lib/src/channel_names.dart` and export it from `lib/neiry_kit.dart`. File contains: `NeiryChannels` (8 MethodChannel IDs), `NeiryEvents` (28 EventChannel IDs), `DeviceLocatorMethods` / `DeviceMethods` / `ClassifierMethods` / `NFBCalibratorMethods` (all method name strings), `NeiryArgs` (9 argument key strings), `NeiryDeviceType` / `NeiryDeviceMode` / `NeiryConnectionState` enums with int mappings. Add unit tests verifying all strings are unique and enum int values match SDK. Full spec: [`.ai-factory/notes/01-channel-contract.md`](.ai-factory/notes/01-channel-contract.md)
- **Dart models**
  - [x] **sentinel helper + exceptions** — `lib/src/models/internal/sentinel.dart` with `orNull(double?)`, `lib/src/models/neiry_exception.dart` with `NeiryException` base + `BluetoothDisabledException` / `DeviceNotConnectedException` / `CalibrationRequiredException`; `NeiryErrorCode` enum (16 values). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **scalar + signal models** — `DeviceInfo` (no nulls), `NeiryError` (for exception construction), `NfbUserState` / `PhysiologicalStatesValue` / `EmotionsStates` (all use `orNull` sentinel pattern on float fields). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **productivity models** — `ProductivityMetrics` (11 nullable floats + `Uint8List?` artifacts, `fatigueGrowthRate` int enum), `ProductivityIndexes` (6 nullable floats + `relaxation`/`stress` int enums + `hasArtifacts` bool), `ProductivityBaselines` (6 nullable floats). Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **calibration models** — `IndividualNfbData` (all non-nullable, defaults from spec), `CalibrationStage` enum, `CalibrationFailReason` enum; `CalibrationEvent` sealed class with subtypes `CalibrationStageFinished(stage)` and `CalibrationCompleted(data)` — native sends `{'type':'stage'/'done'}` over one EventChannel, Dart deserializes to correct subtype; only `IndividualNfbData` has `fromMap`. Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **export + unit tests** — export all models from `lib/neiry_kit.dart`; tests cover sentinel→null, `Uint8List?` size=0 case, enum int mappings, `CalibrationEvent` dispatch by `type` key, `IndividualNfbData.fromMap` round-trip. Spec: [`.ai-factory/notes/02-dart-models.md`](.ai-factory/notes/02-dart-models.md)
  - [x] **PhysiologicalStatesBaselines model + PhysioClassifier fix** — add `lib/src/models/physio_baselines.dart`: `PhysiologicalStatesBaselines` with fields `timestamp: DateTime?`, `alpha/beta/alphaGravity/betaGravity/concentration: double?`, `fromMap` using `orNull` sentinel pattern, `toMap` with keys `ts/alpha/beta/alphaGravity/betaGravity/concentration`; change `PhysioClassifier.calibrated` from `Stream<Uint8List>` to `Stream<PhysiologicalStatesBaselines>`; change `PhysioClassifier.importBaselines(Uint8List)` to `importBaselines(PhysiologicalStatesBaselines)` — passes `baselines.toMap()` to the bridge; export from `lib/neiry_kit.dart`; update iOS and Android bridges to emit baselines as a map of 6 fields (not bytes) from the `CalibratedHandler` callback; add `fromMap` round-trip unit test. Detail: [`.ai-factory/notes/15-explore-example-physio-baselines.md`](.ai-factory/notes/15-explore-example-physio-baselines.md)
- **Dart API**
  - [x] **DeviceLocator** — singleton factory with optional `logDirectory`; `requestDevices({type, searchTime})` returns `Stream<List<DeviceInfo>>` that emits once then completes; `createDevice(serial)`, `update()` (single-threaded mode), `dispose()`. Spec: [`.ai-factory/notes/03-dart-api-device.md`](.ai-factory/notes/03-dart-api-device.md); `requestDevices` must pass arguments through `receiveBroadcastStream()` with no separate MethodChannel call — the native scan starts inside `StreamHandler.onListen`; `createDevice` must invoke on `NeiryChannels.deviceLocator` (not `NeiryChannels.device`) because the C API requires the locator handle; all public methods must call `_checkNotDisposed()` and throw `StateError` when disposed; `requestDevices` must use a `StreamController` internally so `_scanSubscription` is set immediately, enabling cancel-on-overlap and cancel-in-dispose; `StreamController.onCancel` and `onDone` must scope to a local `thisSub` variable and use `identical(_scanSubscription, thisSub)` before clearing the field, so cancelling an old consumer's stream cannot kill a newer active scan; must include `setSingleThreaded(bool)` so `update()` is reachable — C SDK defaults to multi-threaded mode where `Update()` is a no-op without `SetSingleThreaded(true)`
  - [x] **Device** — lifecycle (`connect/disconnect/start/stop`); state streams (`connectionStateStream`, `modeChangedStream`); data streams (`eegStream`, `psdStream`, `artifactsStream`, `resistanceStream`, `batteryStream`, `errorStream`); getters (`getInfo`, `battery`, `mode`, `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`, `getChannelNames`, `getChannelIndex`); enforces state machine invariants (`start()` before classifiers, `connect()` before `start()`). Spec: [`.ai-factory/notes/03-dart-api-device.md`](.ai-factory/notes/03-dart-api-device.md)
  - [x] **NfbClassifier** — `factory NfbClassifier(Device, {IndividualNfbData?})`; streams: `stateStream`, `errorStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **PhysioClassifier** — `factory PhysioClassifier(Device)`; `startBaselineCalibration()`, `importBaselines(Uint8List)`; streams: `stateStream`, `calibrationProgress`, `calibrated`, `individualNfbStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **EmotionsClassifier** — `factory EmotionsClassifier(Device)`; streams: `stateStream`, `errorStream`; no calibration required. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **ProductivityClassifier** — two factory paths (`ProductivityClassifier(Device)`, `ProductivityClassifier.withCalibration(Device, IndividualNfbData)`); `startBaselineCalibration()`, `importBaselines()`, `resetAccumulatedFatigue()`; streams: `baselineStream`, `indexesStream`, `metricsStream`, `calibrationProgress`, `calibrated`, `individualNfbStream`. Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **CardioClassifier** — two factory paths (`CardioClassifier(Device)`, `CardioClassifier.withCalibration(Device, IndividualNfbData)`); streams: `stateStream` (`CardioData`: heartRate, stressIndex, kaplanIndex, quality flags), `ppgStream`, `errorStream`; pure C API (`clCCardio_*`). Spec: [`.ai-factory/notes/04-dart-api-classifiers.md`](.ai-factory/notes/04-dart-api-classifiers.md)
  - [x] **NfbCalibrator** — static class; `calibrateIndividual()` → `Stream<CalibrationEvent>`; `calibrateIndividualQuick()` → `Future<IndividualNfbData>`; `importCalibrationData()`, `getCalibrationData()`, `isCalibrated()`. Spec: [`.ai-factory/notes/05-nfb-calibration.md`](.ai-factory/notes/05-nfb-calibration.md)
  - [x] **export + integration tests** — export all classes; tests: state machine throws on wrong order, stream types match models, `CalibrationEvent` sealed dispatch

- **iOS bridges**
  - [x] **bridge setup + spike** — create `ios/Classes/NeiryKitBridge.h` (umbrella header with `#include "CCapsuleAPI.h"`); add `s.public_header_files` to podspec; rewrite `NeiryKitPlugin.swift` to register all 8 MethodChannels + 22 EventChannels with bridge stubs; call `clCCapsule_GetVersionString()` to verify compilation. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceLocatorBridge** — `OpaquePointer?` to `clCDeviceLocator`; methods: `requestDevices`, `createDevice`, `setSingleThreaded`, `dispose`; EventChannel `deviceList` with `DispatchQueue.main.async` dispatch; `deinit` → `clCDeviceLocator_Destroy`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceBridge — lifecycle + getters** — `connect/disconnect/start/stop`; getters: `getInfo`, `getBatteryCharge` (takes `clCError*`), `getMode` (no `clCError*` — must NOT be wrapped in `do/catch checkCError`), `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`; channel name accessors require two-step `clCDevice_GetChannelNames` handle before calling `GetChannelIndexByName`/`GetChannelNameByIndex`/`GetChannelsCount`; `connect` extracts `bipolarChannels` from args in addition to `serial`; device handle cleanup coordinated through `NeiryKitPlugin` — bridges must never cross-call each other; supports only one active device at a time; `deinit` → `clCDevice_Release`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceBridge — streams** — 8 EventChannels: `eeg`, `psd`, `artifacts`, `resistance`, `battery`, `error`, `connectionStatus`, `mode`; stream handlers must be per-channel `DeviceStreamHandler` inner classes so `onListen` receives the channel ID; all C callbacks dispatch to `DispatchQueue.main.async`; all C accessor functions inside callbacks take `clCError*` including `clCPSDData_HasIndividualAlpha/Beta(data, &error)` — if any accessor fails, skip the entire event. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **NfbBridge** — `clCNFB` handle; EventChannels: `nfbState` (NfbUserState fields), `nfbError`; `clCNFB_Create` and `clCNFB_CreateCalibrated` (takes calibrator handle from `clCNFBCalibrator_CreateOrGet`); no Destroy; MethodChannel dispatch shared on `neiry_kit/device`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **NfbCalibratorBridge** — `clCNFBCalibrator_CreateOrGet(device)` — no `clCError*`, returns cached handle, do NOT destroy; separate MethodChannel `neiry_kit/nfb_calibrator` + EventChannel `neiry_kit/events/nfbCalibration`; stage-finished callback receives only handle with no stage number — bridge maintains `currentStage: Int`, increments on each callback and manually calls next stage; `calibrateIndividualQuick` holds MethodChannel `result` sink and calls it from `onCalibrated` — do NOT emit on EventChannel for quick mode; `IsCalibrated`/`HasCalibrationFailed` return bool with no `clCError*` — do NOT wrap in `do/catch checkCError`; calibration EventChannel emits `{'type':'stage','stage':N}` and `{'type':'done',...IndividualNfbData}`. Detail: [`.ai-factory/notes/08-explore-nfb-calibrator.md`](.ai-factory/notes/08-explore-nfb-calibrator.md)
  - [x] **EmotionsBridge** — `clCEmotions` handle; EventChannels: `emotionsState` (EmotionsStates fields), `emotionsError`; `clCEmotions_SetOn*Event` take NO `clCError*` — do NOT wrap in `do/catch checkCError`; no calibration, no Destroy. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **PhysioBridge** — `clCPhysiologicalStates` handle; `clCPhysiologicalStates_SetOn*Event` ALL take `clCError*` — wrap in `do/catch checkCError`; `StartBaselineCalibration` has no error param; `ImportBaselines` takes `const clCPhysiologicalStates_Baselines*` struct — bridge deserializes Dart map (6 keys: `ts/alpha/beta/alphaGravity/betaGravity/concentration`) to C struct; `CalibratedHandler` callback serializes all 6 struct fields to Dart map; `IndividualNFBUpdateHandler` receives only handle with no data — emit empty signal map `{}`; `PhysiologicalStates_Value` has `nfbArtifacts` + `cardioArtifacts` bool fields — include in emitted map; no Destroy. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [x] **CardioBridge — post-review fix** — one bug passed review incorrectly: PPG callback (`CardioBridge.swift`) does not guard `ppgData` for nil before passing to `clCPPGTimedData_GetCount` — Swift imports the C callback param as `OpaquePointer?`, nil would trap at runtime; add `let ppgData = ppgData` to the existing `guard let bridge` statement in the `SetOnPPGDataEvent` closure.
  - [x] **CardioBridge** — `clCCardio` handle; two factory paths: `clCCardio_Create` and `clCCardio_CreateCalibrated(device, calibrator)` where calibrator = `clCNFBCalibrator_CreateOrGet`; EventChannels: `cardioState` (direct struct fields: `heartRate/stressIndex/kaplanIndex/hasArtifacts/skinContact/motionArtifacts/metricsAvailable`), `cardioPpg` (PPG uses accessor functions: `GetCount/GetValue/GetTimestampMilli`), `cardioError`; `CardioData` bool fields initialized to `0.F` — read normally; `SetOnIndexesUpdateEvent` and `SetOnCalibratedEvent` take `clCError*`; no Destroy. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)
  - [x] **ProductivityBridge** — `clCProductivity` handle; two factory paths: `clCProductivity_Create` and `clCProductivity_CreateWithIndividualData(device, &data)` — bridge converts Dart `IndividualNfbData` map back to C struct; EventChannels: `productivityMetrics` (`clCProductivity_Metrics`: 11 floats + `artifactsData` as `Uint8List`), `productivityIndexes` (`clCProductivity_Indexes`: relaxation enum 0–5, stress enum 0–2), `productivityBaseline`, `productivityCalibrationProgress`, `productivityCalibrated`, `productivityIndividualNfb`; ALL `clCProductivity_SetOn*Event` take NO `clCError*`; `StartBaselineCalibration` also has no error param; `resetAccumulatedFatigue` takes `clCError*`; no Destroy. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)

- **Android bridges**
  - [x] **build setup + spike** — Path B (custom JNI wrapper): extract `libCapsuleClient.so` + `libc++_shared.so` from `CapsuleService.aar/jni/arm64-v8a/` to `android/src/main/jniLibs/arm64-v8a/`; keep `fileTree` dependency for both AARs; add `CMakeLists.txt` building `neiry_jni` shared lib linking against extracted `.so`; add `externalNativeBuild` block to `build.gradle.kts`; add `external fun nativeGetVersion(): String` to `NeiryKitPlugin.kt`; call `System.loadLibrary("neiry_jni")` in companion object; implement `nativeGetVersion()` JNI stub in `jni_bridge.cpp` calling `clCCapsule_GetVersionString()` to verify linking; register all 8 MethodChannels + 22 EventChannels with stubs. `devicedriver.aar` is pure Kotlin — no `.so` extraction needed. arm64-v8a only — no simulator. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [x] **DeviceLocatorBridge** — `jni_device_locator.cpp` + `DeviceLocatorBridge.kt`; `jlong` handle; methods: `requestDevices`, `createDevice`, `setSingleThreaded`, `dispose`; EventChannel `deviceList` via `Handler(Looper.getMainLooper()).post`; all `external fun` declarations on a single `NativeBridge` class (not on `DeviceLocatorBridge`) — centralized per notes/11; `FlutterError(code, message, details): Exception` must be defined as a shared class (Android has no built-in FlutterError throwable); error propagation: JNI encodes `clCError` as `"<code>|<message>"` string, Kotlin splits on first `|` via top-level `parseSdkError()` to get numeric code for `result.error(code, message, details)` — preserves `NeiryErrorCode` mapping on Dart side; `g_deviceListCallback` guarded by `pthread_mutex_t` — copy to local under lock before JNI calls, null-check after; `DetachCurrentThread` called on ALL return paths in C callback including early-return when callback is NULL — use `goto cleanup`; `marshal_device_list` checks `clCError*` on every C SDK call (`GetCount`, `GetDeviceInfo`), skips entries on failure; C function is `clCDeviceInfo_GetType` (NOT `clCDeviceInfo_GetDeviceType`); `FindClass` paths use literal `neiry_kit` (NOT `neiry_1kit` — `_1` encoding applies only to `Java_` function name prefixes, not `FindClass`); `createDevice` and `onListen` must guard `handle != 0L` and throw `FlutterError("NO_LOCATOR", ...)` when locator not created; `g_jvm` initialized in `nativeCreate` (first JNI call in lifecycle), not lazily; null-safe dispatch in plugin via `val bridge = deviceLocatorBridge ?: return result.error("NOT_INITIALIZED", ...)`. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [x] **DeviceLocatorBridge — post-review fixes** — two issues that passed code review incorrectly: (1) in `on_device_list_callback` (`jni_device_locator.cpp`), replace raw pointer copy of `g_deviceListSink`/`g_handler` under mutex with `NewLocalRef` — raw copy is a use-after-free if `DeleteGlobalRef` runs concurrently on `nativeSetDeviceListSink(null)`; delete local refs after use; (2) `DeviceLocatorBridge.update()` must throw `FlutterError("NO_LOCATOR", "DeviceLocator not created", null)` when `handle == 0L` — iOS parity: `DeviceLocatorBridge.swift` throws when `locator` is nil, Android must match.
  - [ ] **DeviceBridge — lifecycle + getters** — `jni_device.cpp` + `DeviceBridge.kt`; `connect/disconnect/start/stop`; getters: `getBatteryCharge`, `getMode` (no `clCError*`), `getEEGSampleRate`, `getPPGSampleRate`, `getMEMSSampleRate`, `getPPGIrAmplitude`, `getPPGRedAmplitude`; channel names require two-step `clCDevice_GetChannelNames` handle pattern; device handle as `jlong` cast via `(jlong)(uintptr_t)`; `connect` extracts `bipolarChannels` from args; all `external fun` on `NativeBridge` (established in DeviceLocatorBridge); reuse `FlutterError` + `parseSdkError()` from DeviceLocatorBridge; null-safe dispatch in plugin via `val bridge = deviceBridge ?: return result.error("NOT_INITIALIZED", ...)`; `FindClass` paths use literal `neiry_kit` (not `neiry_1kit`). Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md); JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [ ] **DeviceBridge — streams** — 8 EventChannels: `eeg`, `psd`, `artifacts`, `resistance`, `battery`, `error`, `connectionStatus`, `mode`; C callbacks have no context param — use static device registry keyed by `clCDevice` pointer; all callbacks post to `Handler(Looper.getMainLooper()).post`; PSD accessors including `HasIndividualAlpha/Beta` all take `clCError*` — skip event on error; global callback refs (EventSinks) guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy) to prevent use-after-free if `DeleteGlobalRef` runs concurrently; delete local refs after use; `DetachCurrentThread` called on ALL return paths in C callbacks — use `goto cleanup`; `FindClass` paths use literal `neiry_kit`. JNI patterns: [`.ai-factory/notes/11-explore-android-jni.md`](.ai-factory/notes/11-explore-android-jni.md)
  - [ ] **NfbBridge** — `jni_nfb.cpp` + `NfbBridge.kt`; EventChannels: `nfbState`, `nfbError`; two factory paths mirroring iOS; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths. Spec: [`.ai-factory/notes/06-native-bridges.md`](.ai-factory/notes/06-native-bridges.md)
  - [ ] **NfbCalibratorBridge** — `jni_nfb_calibrator.cpp` + `NfbCalibratorBridge.kt`; same stage-tracking, quick-mode, and no-Destroy rules as iOS; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink ref guarded by `pthread_mutex_t`; `DetachCurrentThread` on all callback return paths. Detail: [`.ai-factory/notes/08-explore-nfb-calibrator.md`](.ai-factory/notes/08-explore-nfb-calibrator.md)
  - [ ] **EmotionsBridge** — `jni_emotions.cpp` + `EmotionsBridge.kt`; `clCEmotions_SetOn*Event` take NO `clCError*`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [ ] **PhysioBridge** — `jni_physio.cpp` + `PhysioBridge.kt`; `clCPhysiologicalStates_SetOn*Event` ALL take `clCError*`; baselines serialized as map of 6 floats (same keys as iOS); `IndividualNFBUpdateHandler` emits `{}`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths. Detail: [`.ai-factory/notes/10-explore-physio-emotions.md`](.ai-factory/notes/10-explore-physio-emotions.md)
  - [ ] **CardioBridge** — `jni_cardio.cpp` + `CardioBridge.kt`; two factory paths (plain + calibrated via `clCNFBCalibrator_CreateOrGet`); EventChannels: `cardioState`, `cardioPpg` (PPG accessor pattern), `cardioError`; `CardioData` bool fields cast `(jboolean)(data->field != 0)`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)
  - [ ] **ProductivityBridge** — `jni_productivity.cpp` + `ProductivityBridge.kt`; two factory paths (plain + `CreateWithIndividualData` — convert Dart map back to C struct); EventChannels: `productivityMetrics`, `productivityIndexes`, `productivityBaseline`, `productivityCalibrationProgress`, `productivityCalibrated`, `productivityIndividualNfb`; ALL `SetOn*Event` take NO `clCError*`; `resetAccumulatedFatigue` takes `clCError*`; no Destroy; all `external fun` on `NativeBridge`; reuse `FlutterError` + `parseSdkError()`; global EventSink refs guarded by `pthread_mutex_t` — use `NewLocalRef` under lock (not raw pointer copy); `DetachCurrentThread` on all callback return paths. Detail: [`.ai-factory/notes/07-explore-cardio-productivity.md`](.ai-factory/notes/07-explore-cardio-productivity.md)

- **Example app**
  - [ ] **app scaffold** — add `riverpod: ^3.3.0`, `flutter_riverpod: ^3.3.0`, `go_router: ^14.0.0`, `rxdart: ^0.27.0`, `file_picker: ^8.0.0`, `path_provider: ^2.1.5`, `wakelock_plus: ^1.2.0` to `example/pubspec.yaml`; wrap `main.dart` in `ProviderScope`; use `StatefulShellRoute.indexedStack` (NOT `ShellRoute`) — each branch gets its own `Navigator`, keeping all tab widgets mounted so live streams survive tab switches; 4 tabs: `/device`, `/streams`, `/classifiers`, `/calibration`; the 5 UI sections from later milestones nest inside these 4 tabs (Productivity+Cardio inside `/classifiers` as sub-page); use `NavigationBar` (Material 3); create `deviceLocatorProvider` as `NotifierProvider` with `ref.onDispose`; all screens empty `Scaffold` stubs. Detail: [`.ai-factory/notes/12-explore-example-navigation.md`](.ai-factory/notes/12-explore-example-navigation.md)
  - [ ] **Device tab** — `deviceScanProvider` as `FutureProvider.family` wrapping `requestDevices().first` (one-shot stream, NOT `StreamProvider`); `activeDeviceProvider` as `NotifierProvider<Device?>` — stores single device, enforces one-device-at-a-time; `deviceConnectionStateProvider` as `StreamProvider`; `deviceIsStartedProvider` as `StateProvider<bool>` — set manually on `start()`/`stop()` (not from stream); `deviceUiStateProvider` composites both into `DeviceUiState` enum (`idle/connected/started`); button enabled state derived from composite; all stream subscriptions in `NotifierProvider`, NOT in `StreamBuilder` widgets (streams must survive tab switches via `StatefulShellRoute`); async disposal: `stop()` → `disconnect()` → `dispose()` order enforced in app `State.dispose()` before `ProviderContainer.dispose()` (`ref.onDispose` is synchronous — cannot await). Detail: [`.ai-factory/notes/13-explore-example-device-providers.md`](.ai-factory/notes/13-explore-example-device-providers.md)
  - [ ] **Streams tab** — `eegProvider` as throttled `StreamProvider` (`rxdart` `throttleTime(100ms)` → 10 Hz); `psdProvider` throttled at 500ms; `resistanceMapProvider` as `NotifierProvider<Map<String, double>>` accumulating per-channel events (resistance fires per-channel independently, not as a batch); `batteryProvider` throttled at 1000ms; `artifactsProvider` unthrottled (bool flags need immediate feedback); all subscriptions in providers NOT `StreamBuilder` widgets; EEG display shows last sample per channel + channel count; PSD shows band powers (delta/theta/alpha/smr/beta); resistance shows channel→kΩ table with color indicator (green if < 500 kΩ); throttle BEFORE any expensive map/transform operations. Detail: [`.ai-factory/notes/16-explore-example-streams-tab.md`](.ai-factory/notes/16-explore-example-streams-tab.md)
  - [ ] **NFB tab + calibration** — `CalibrationNotifier` as `AsyncNotifier<IndividualNfbData?>` handling full (stream) + quick (future) + import/export; `CalibrationTimerNotifier` as `StateNotifier` with `Timer.periodic` — timer is purely client-side, `CalibrationEvent` stream emits only on stage END not ticks; start timer for stage 1 at call time (before first event), reset on each `CalibrationStageFinished`; `CalibrationUiState` as sealed class (`CalibrationIdle / CalibrationStageActive(stage, elapsed) / CalibrationDone / CalibrationError`); stage instruction text: stage1+3 = "Close eyes and relax", stage2+4 = "Open eyes, look straight ahead"; `wakelock_plus` enabled before calibration, disabled in finally/abort; `NfbClassifier` provider returns null if device not started — widget shows "Waiting for device"; completed `IndividualNfbData` stored in shared `nfbCalibrationProvider` so Productivity+Cardio tab can use it; Import/Export via `file_picker` + `path_provider` as JSON (`data.toMap()`). Detail: [`.ai-factory/notes/14-explore-example-calibration-ui.md`](.ai-factory/notes/14-explore-example-calibration-ui.md)
  - [ ] **Physio + Emotions tab** — requires **PhysiologicalStatesBaselines model + PhysioClassifier fix** milestone above; Import/Export baselines as JSON (`PhysiologicalStatesBaselines.toMap()`/`fromMap()`); Physio fires every ~2 min — show "Last updated: HH:MM:SS" label, reduce opacity to 0.5 while waiting for first update; `PhysiologicalStatesValue` has `nfbArtifacts` + `cardioArtifacts` bool fields — show as "Signal Quality" row with green/red icons below numeric values; Emotions fires continuously — separate `Card` above Physio card with no timestamp. Detail: [`.ai-factory/notes/15-explore-example-physio-baselines.md`](.ai-factory/notes/15-explore-example-physio-baselines.md)
  - [ ] **Productivity + Cardio tab** — shared `nfbCalibrationProvider` as `StateNotifierProvider<IndividualNfbData?>` — set by Calibration tab, watched here; factory `Switch` disabled if `nfbCalibrationProvider` is null; toggling switch invalidates classifier provider to recreate (safe: classifiers have no Destroy, creating a new instance while old exists is OK — SDK manages handles); `ProductivityIndexes.relaxation` = `clCProductivity_RecommendationValue` enum (0–5: NoRecommendation/Involvement/Relaxation/SlightFatigue/SevereFatigue/ChronicFatigue) — show label, not int; `ProductivityIndexes.stress` = `clCProductivity_StressValue` (0–2: NoStress/Anxiety/Stress) — show label; `ProductivityMetrics.artifactsData` is opaque blob — show size only ("Artifacts: N bytes" or "none"); PPG last value = `ppgData.values.last` + `ppgData.timestamps.last` as formatted time; all classifier providers return null when device not started. Detail: [`.ai-factory/notes/17-explore-example-productivity-cardio-tab.md`](.ai-factory/notes/17-explore-example-productivity-cardio-tab.md)
